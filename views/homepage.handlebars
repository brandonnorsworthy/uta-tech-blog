<ul>
    {{#each posts as |post|}}
    <li>
        <div>
            <h3>{{post.title}}</h3>
            <p>Posted by {{post.user.username}} on {{post.date_created}}</p>
        </div>
        <p>{{post.content}}</p>
    </li>
    {{/each}}
</ul>